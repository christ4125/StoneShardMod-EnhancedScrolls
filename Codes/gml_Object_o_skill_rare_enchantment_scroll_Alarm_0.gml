// Alarm_0 — fires after the scroll's dissolve animation finishes.
// event_user(0) is what actually destroys/removes the scroll instance.
// This is the cleanup step after sh_diss = 200 plays out.
event_user(0);
